apiVersion: v1
kind: Service
metadata:
  name: {{ include "ToppingsApi.fullname" . }}
  labels:
    {{- include "ToppingsApi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ToppingsApi.selectorLabels" . | nindent 4 }}
