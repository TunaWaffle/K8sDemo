apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ToppingsApi.fullname" . }}-test-connection"
  labels:
    {{- include "ToppingsApi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ToppingsApi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
